import { inter } from './fonts';

export const globalStyles = {
  styles: {
    global: () => ({
      body: {
        fontFamily: inter.style.fontFamily,
      },
      html: {
        fontFamily: inter.style.fontFamily,
      },
    }),
  },
};
